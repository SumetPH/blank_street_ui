import 'package:flutter/material.dart';

class StateSecondScreen extends StatelessWidget {
  const StateSecondScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('State Second Screen'),
      ],
    );
  }
}
